<h1>My Category Index</h1> 
Please add categories to your store <%= link_to "adding new categorys", new_category_path,   { :class=>"btn btn-default" } %>
<p>
<ul class="list-group">
	<% @categories.each do |category| %>
	<li class="list-group-item">
		<h4 class="list-group-item-heading"><%= category.title %></h4>
		<p class="list-group-item-text"><%= category.description %></p>		
		Link: <%= link_to "Read me", category_path(category)%>
	</li>
	<% end %>
</ul>
