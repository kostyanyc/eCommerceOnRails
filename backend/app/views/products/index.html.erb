<h1>My Products Index</h1> 
Please add products to your store <%= link_to "adding new products", new_product_path,   { :class=>"btn btn-default" } %>
<p>
<ul class="list-group">
	<% @products.each do |product| %>
	<li class="list-group-item">
		<h4 class="list-group-item-heading"><%= product.title %></h4>
		<p class="list-group-item-text"><%= product.description %></p>		
		Link: <%= link_to "Read me", product_path(product)%>
	</li>
	<% end %>
</ul>
